export interface Rule {
  title: string;
  fullDescription: string;
  area: string;
  discipline: string;
  skill: string;
}

export interface FilterState {
  area: string;
  discipline: string;
  skill: string;
}
